```<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <script>
        alert("dkssudgkrpty")//alert('쓸말')을 하면 창을 띄울수 있다
        confirm("메세지")//confirm("쓸말")을 하면 취소버튼이 생긴다
        prompt("이름입력","기본값")//prompt("질문")을 하면 입력을 할수 있게 된다
        document.write("<h1>화면에출력</h1>")//document객체는 아직 안배웠고 웹문서(document)에 내용(write)안에 내용을 출력하는 것이다.html태그 사용도 가능하다
        var name= prompt("이름을 입력하시오")//var는 변수를 줄수 있다
        document.write("<b>"+name+"</b>님,환영합니다")//+는 연결연산자로 붙여서 써줄수 있게 해준다
        var name2=prompt("콘솔창에 할거")
        console.log(name + "님 어서오시게")//console.log는 콘솔창에 출력하게 한다.
    </script>
</body>
</html>```
